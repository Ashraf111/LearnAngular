export class Unit {
 static  calculateUnit(totalUnitCount:any,):any {
       
    //     let minutes = 0;
    //      totalUnitCount = totalUnitCount.toString()
    //     totalUnitCount = totalUnitCount.slice(-1)
    //     if (totalUnitCount == "0" ) {
    //       minutes = parseInt(minutes) - minutePerUnit * 2
    //     }
    
    //     else {
    //       minutes = parseInt(minutes)
    //     }
    //     let currentHours = parseInt(hours);
    //     let totalMinutesWorked = (currentHours > 0 ? currentHours * 60 : 0) + minutes;
    //     if (totalMinutesWorked >= minutePerUnit) {
    //       let totalUnit = totalMinutesWorked / minutePerUnit;
    //       totalUnit = totalUnit - Math.floor(totalUnit)
    //       if (seconds == "00" && editMode == true && totalUnit == 0) {
    //         totalUnitCount = Math.floor(totalMinutesWorked / minutePerUnit);
    //       }
    //       else {
    //         totalUnitCount = Math.floor(totalMinutesWorked / minutePerUnit) + 1;
    //       }
    
    //     }
    
    //     else totalUnitCount = 1;
       
      };
}
